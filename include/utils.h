#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <vector>

namespace utils{
std::string formatNumber(double number);
std::vector<int> generateRange(int start,int end);
    bool isPrime(int number);}  // namespace utils
#endif  // UTILS_H